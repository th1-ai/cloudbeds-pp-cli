# Compiled binaries (root-anchored so cmd/cloudbeds-pp-cli/ source dir isn't ignored)
/cloudbeds-pp-cli
/cloudbeds-pp-mcp
/cloudbeds
/build/
/dist/

# Test artifacts (regenerated by printing-press tooling)
dogfood-results.json
workflow-verify-report.json

# Local data / cache (created at runtime, never committed)
*.db
*.db-journal

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
